from setuptools import setup

setup(name='dc-mixer',
      version='0.1',
      description='Package which allow to run docker-compose with several docker-compose.yml files',
      url='https://github.com/paunin/docker-compose-mixer',
      author='Dmitriy Paunin',
      author_email='d.m.paunin@gmail.com',
      license='MIT',
      packages=['dc-mixer'],
      zip_safe=False)
